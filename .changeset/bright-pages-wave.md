---
'@questionable-fyi/app': minor
---

Add subscript and superscript formatting to the richtext editor.

Two new toolbar affordances let authors mark text as subscript (`<sub>`) or superscript (`<sup>`). Subscript and superscript are mutually exclusive — toggling one when the other is active replaces it. Both round-trip through the lexicon and render correctly on the read side.
