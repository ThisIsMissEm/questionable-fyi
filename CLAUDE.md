# CLAUDE.md

This is a pnpm monorepo. The primary workspace is `app/` — all application code, frontend and backend, lives there. See `app/CLAUDE.md` for full project guidance.

When started from the repository root, always run pnpm scripts (`pnpm dev`, `pnpm typecheck`, `pnpm lint`, etc.) from the `app/` directory.
